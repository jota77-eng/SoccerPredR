#' Asian Handicap distribution (initial skeleton)
#' @export
handicap_distribution <- function(league, open_line, close_line, data) {
  check_input_data(data)

  # TODO: implement filtering + outcome calculation
  message("handicap_distribution() is under development.")

  return(NULL)
}
