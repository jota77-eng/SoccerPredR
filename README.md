# SoccerPredR

<!-- badges: start -->
<!-- badges: end -->

`SoccerPredR` is an R package designed for analyzing soccer betting market data,
including **Asian Handicap lines** and **European odds**, and preparing features
for simple predictive modeling.  
This package is built as the final project for STAT 600.

---

## 📌 Current Status (Intermediate Project)

This repository currently contains:

- Package structure created using `usethis::create_package()`
- MIT license added
- README added
- Initial function skeletons in the `R/` folder:
  - `handicap_distribution()`
  - `odds_distribution()`
  - `check_input_data()`
- Roxygen2 documentation structure established
- Package can be loaded using `devtools::load_all()`

These components fulfill the requirements for the **Final Intermediate Project**.

---

## 📦 Installation (for Final Project)

The package will be installable with:

```r
# devtools::install_github("**jota77-eng**/SoccerPredR")
